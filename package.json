{
  "name": "pns-sdk",
  "version": "0.8.0",
  "scripts": {
    "compile": "rm -rfv lib/* && tsc -d --project tsconfig.json --outDir lib/",
    "dev": "vite",
    "build": "tsc && vite build",
    "serve": "vite preview"
  },
  "devDependencies": {
    "@types/node": "^15.12.1",
    "typescript": "^4.3.2",
    "vite": "^2.6.0"
  },
  "dependencies": {
    "buffer": "^6.0.3",
    "ethers": "^5.4.7",
    "ws": ">=7.4.6"
  }
}
